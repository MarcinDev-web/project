{
  "name": "@engine/wasm-texture-processor",
  "version": "0.1.0",
  "description": "High-performance WASM texture processing for Play Engine",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist",
    "pkg"
  ],
  "scripts": {
    "build": "tsc",
    "build:wasm": "cd ../../crates/texture-processor && cargo build --release --target wasm32-unknown-unknown && wasm-bindgen --target web --out-dir ../../packages/wasm-texture-processor/pkg target/wasm32-unknown-unknown/release/texture_processor.wasm",
    "build:wasm:simd": "cd ../../crates/texture-processor && RUSTFLAGS='-C target-feature=+simd128' cargo build --release --target wasm32-unknown-unknown --features simd && wasm-bindgen --target web --out-dir ../../packages/wasm-texture-processor/pkg target/wasm32-unknown-unknown/release/texture_processor.wasm",
    "clean": "rimraf dist pkg"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.9.3",
    "rimraf": "^6.1.2"
  },
  "repository": {
    "type": "git",
    "directory": "packages/wasm-texture-processor"
  },
  "keywords": [
    "wasm",
    "texture",
    "procedural",
    "pbr",
    "webgpu"
  ],
  "license": "MIT"
}

