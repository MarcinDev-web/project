{
  "name": "@engine/wasm-voxel",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./src/index.ts",
    "./pkg/voxel_engine.js": "./pkg/voxel_engine.js"
  },
  "files": [
    "dist",
    "pkg",
    "src"
  ],
  "scripts": {
    "build:wasm": "wasm-pack build ../../crates/voxel-engine --target web --out-dir ../../packages/wasm-voxel/pkg",
    "build:wasm:skip": "node -e \"process.exit(require('fs').existsSync('pkg/voxel_engine.js') ? (console.log('Skipping WASM build'), 0) : (console.error('ERROR: Pre-built WASM files not found'), 1))\"",
    "build:ts": "tsc -p tsconfig.json",
    "build": "pnpm run build:wasm:skip && pnpm run build:ts",
    "build:full": "pnpm run build:wasm && pnpm run build:ts",
    "test": "vitest run",
    "clean": "rimraf dist pkg",
    "lint": "eslint src --max-warnings=0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "rimraf": "^6.1.2",
    "typescript": "^5.9.3",
    "vitest": "^4.0.14"
  }
}
