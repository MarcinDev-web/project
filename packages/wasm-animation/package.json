{
  "name": "@engine/wasm-animation",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./src/index.ts",
    "./pkg/animation.js": "./pkg/animation.js"
  },
  "files": [
    "dist",
    "pkg",
    "src"
  ],
  "scripts": {
    "build:wasm": "wasm-pack build ../../crates/animation --target web --out-dir ../../packages/wasm-animation/pkg",
    "build:wasm:skip": "node -e \"process.exit(require('fs').existsSync('pkg/animation.js') ? (console.log('Skipping WASM build - using pre-built WASM files from pkg/'), 0) : (console.error('ERROR: Pre-built WASM files not found in pkg/ and cannot build (cargo missing)'), 1))\"",
    "build:ts": "tsc -p tsconfig.json",
    "build": "pnpm run build:wasm:skip && pnpm run build:ts",
    "build:full": "pnpm run build:wasm && pnpm run build:ts",
    "clean": "rimraf dist pkg",
    "lint": "eslint src --max-warnings=0"
  },
  "devDependencies": {
    "@types/node": "^25.0.6",
    "rimraf": "^6.1.2",
    "typescript": "^5.9.3"
  }
}
